# on the root directory: type `npm i`

# then type `npm run start:dev`

### environment variables are added to the project(for testing purposes only, not ideal for development), no need to add them manually
